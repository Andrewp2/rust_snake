First, download Rust and Cargo from https://www.rust-lang.org/. I'd recommend installing rustup, which will also install cargo.

Then, simply run `cargo run` to run the code.

WASD to control the snake.